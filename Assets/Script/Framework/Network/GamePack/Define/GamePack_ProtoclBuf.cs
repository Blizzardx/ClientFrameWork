using UnityEngine;

namespace Framework.Network.GamePack
{
    public class GamePack_ProtoclBuf : MonoBehaviour {

        // Use this for initialization
        void Start () {
	
        }
	
        // Update is called once per frame
        void Update () {
	
        }
    }
}
